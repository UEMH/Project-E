<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录 - 书签管理</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            margin: 0;
        }
        
        .login-container {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 400px;
        }
        
        h1 { 
            text-align: center; 
            margin-bottom: 1.5rem; 
            color: #333; 
        }
        
        .form-group { 
            margin-bottom: 1rem; 
        }
        
        label { 
            display: block; 
            margin-bottom: 0.5rem; 
            color: #555; 
            font-weight: bold;
        }
        
        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            box-sizing: border-box;
        }
        
        input:focus {
            border-color: #4CAF50;
            outline: none;
            box-shadow: 0 0 5px rgba(76, 175, 80, 0.3);
        }
        
        button {
            width: 100%;
            padding: 12px;
            background: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 1rem;
            font-weight: bold;
            transition: background 0.3s;
        }
        
        button:hover { 
            background: #45a049; 
        }
        
        button:disabled {
            background: #cccccc;
            cursor: not-allowed;
        }
        
        .error { 
            color: #d32f2f; 
            text-align: center; 
            margin-bottom: 1rem;
            padding: 10px;
            background: #ffebee;
            border-radius: 5px;
            border: 1px solid #f44336;
        }
        
        .links { 
            text-align: center; 
            margin-top: 1rem; 
        }
        
        .links a { 
            color: #667eea; 
            text-decoration: none;
            font-weight: bold;
        }
        
        .links a:hover {
            text-decoration: underline;
        }
        
        .default-account {
            margin-top: 1rem;
            padding: 15px;
            background: #f5f5f5;
            border-radius: 5px;
            text-align: center;
            border: 1px solid #e0e0e0;
        }
        
        .default-account p {
            margin: 0;
            font-size: 0.9rem;
            color: #666;
            line-height: 1.5;
        }
        
        .database-status {
            margin-bottom: 1rem;
            padding: 12px;
            border-radius: 5px;
            text-align: center;
            background: #fff3cd;
            border: 1px solid #ffeaa7;
        }
        
        .database-status p {
            margin: 0;
            color: #856404;
            font-size: 0.9rem;
            line-height: 1.4;
        }
        
        .offline-info {
            margin-top: 1rem;
            padding: 12px;
            background: #e7f3ff;
            border-radius: 5px;
            text-align: center;
            border: 1px solid #b3d9ff;
        }
        
        .offline-info p {
            margin: 0;
            font-size: 0.8rem;
            color: #0066cc;
            line-height: 1.4;
        }
        
        .success-info {
            margin-top: 1rem;
            padding: 12px;
            background: #d4edda;
            border-radius: 5px;
            text-align: center;
            border: 1px solid #c3e6cb;
        }
        
        .success-info p {
            margin: 0;
            font-size: 0.8rem;
            color: #155724;
            line-height: 1.4;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>用户登录</h1>
        
        <!-- 数据库连接状态提示 -->
        <% if (!dbConnected) { %>
        <div class="database-status">
            <p>
                ❌ <strong>数据库连接失败</strong><br>
                无法登录，请检查数据库连接或联系管理员
            </p>
        </div>
        <% } else { %>
        <div class="success-info">
            <p>
                ✅ <strong>数据库连接正常</strong><br>
                可以正常登录和注册
            </p>
        </div>
        <% } %>
        
        <% if (error) { %>
            <div class="error">
                <strong>错误:</strong> <%= error %>
            </div>
        <% } %>
        
        <form action="/login" method="POST" id="loginForm">
            <div class="form-group">
                <label for="username">用户名:</label>
                <input type="text" id="username" name="username" required 
                       placeholder="请输入用户名" value="<%= typeof username !== 'undefined' ? username : 'UEMH-CHAN' %>">
            </div>
            <div class="form-group">
                <label for="password">密码:</label>
                <input type="password" id="password" name="password" required 
                       placeholder="请输入密码" value="<%= typeof password !== 'undefined' ? password : '041018' %>">
            </div>
            <button type="submit" id="loginButton" <%= !dbConnected && 'disabled' %>>
                <% if (!dbConnected) { %>
                    数据库连接失败
                <% } else { %>
                    登录
                <% } %>
            </button>
        </form>
        
        <div class="links">
            <a href="/register">没有账号？立即注册</a>
        </div>
        
        <div class="default-account">
            <p>
                <strong>默认管理员账号:</strong><br>
                用户名: <strong>UEMH-CHAN</strong><br>
                密码: <strong>041018</strong>
            </p>
        </div>
        
        <% if (!dbConnected) { %>
        <div class="offline-info">
            <p>
                ⚠️ <strong>系统暂时无法使用</strong><br>
                数据库连接异常，请检查MongoDB连接配置
            </p>
        </div>
        <% } %>
    </div>

    <script>
        // 表单验证
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value;
            const button = document.getElementById('loginButton');
            
            if (!username || !password) {
                e.preventDefault();
                alert('请输入用户名和密码');
                return;
            }
            
            // 显示加载状态
            button.innerHTML = '登录中...';
            button.disabled = true;
        });
        
        // 自动聚焦到用户名输入框
        document.getElementById('username').focus();
    </script>
</body>
</html>
