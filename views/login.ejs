<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录 - 书签管理</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="login-container">
        <h1>用户登录</h1>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">用户名:</label>
                <input type="text" id="username" name="username" required value="UEMH-CHAN">
            </div>
            <div class="form-group">
                <label for="password">密码:</label>
                <input type="password" id="password" name="password" required value="041018">
            </div>
            <button type="submit">登录</button>
        </form>
        <div class="links">
            <a href="/register">没有账号？立即注册</a>
        </div>
        <div class="default-account" style="margin-top: 1rem; padding: 0.5rem; background: #f5f5f5; border-radius: 5px; text-align: center;">
            <p style="margin: 0; font-size: 0.9rem; color: #666;">默认账号: UEMH-CHAN / 041018</p>
        </div>
    </div>
</body>
</html>
